<h1> Show from post</h1>

<h2><%= @post.title %>

<div>
<p>
<%= @post.body %>
</div>
 <%= link_to 'Delete', post_path(@post), method: :delete , data: {confirm:' Are you  sure'}%>
<%= link_to 'Update' , edit_post_path(@post) %>
</p>
